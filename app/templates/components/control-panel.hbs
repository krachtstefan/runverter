{{#if visible}}
  <a href="#" {{action "toggleControlPanel"}}>hide control panel</a>

  <a href="#" {{action "toggleLanguage"}}>{{t "controlPanel.switchLanguage"}}</a>
{{else}}
  <a href="#" {{action "toggleControlPanel"}}>show control panel</a>
{{/if}}
