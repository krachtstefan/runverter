{{#unless flashMessages.isEmpty}}
  <div class="notification-messages uk-container-center">
    {{#each flashMessages.queue as |flash|}}
      {{flash-message flash=flash}}
    {{/each}}
  </div>
{{/unless}}